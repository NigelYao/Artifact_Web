# 实现与验证记录

## 结构

游戏用普通 HTML、CSS 与 JavaScript 实现。全部资源本地加载，无 CDN、后端、API 密钥、在线素材抓取或依赖安装。

`Game` 为独立于浏览器的规则对象。`s` 是可序列化状态，包含种子、单位、牌库、手牌、金币、三路防御建筑、独立魔力池、强化、回合、先手、事件和胜负。随机行为使用状态内的 xorshift32。

主操作为 `newGame / play / activate / pass / buy / hold / startRound / deploy / finishDeployment`。`play` 和 `activate` 在目标无效时回滚状态，避免扣除魔力却没有完成施放。

单位保存伤害值、永久及临时修正、装备、冷却与战斗目标。`stats()` 计算当前属性及主要光环，`combatForecast()` 计算直接伤害快照，`combat()` 施加同时战斗伤害，再处理阵亡及后续效果。

每张法术使用显式分支，不用自然语言关键字猜测技能。`targetSpec` 同时驱动界面提示、有效目标校验和电脑候选目标。电脑通过同一套操作 API 试算合法动作，对单位、建筑、金币和手牌评分。

音频通过 AudioContext 的振荡器、滤波器、包络和卷积混响合成。没有官方原声资产；初次手势启用，切到后台时挂起。

## 验证结果

执行环境：Node.js 24，Python 3。未进行浏览器实机或画面截图验收。

23 组引擎检查通过，覆盖：

- 303 条唯一卡牌、48 位英雄、完整本地画作、签名关联和四套合法预设牌组。
- 118 张法术的显式处理分支、每张法术和消耗品的一次代表性有效施放。
- 全部登记的主动技能的代表性有效执行。
- 正护甲、负护甲、穿刺、免疫、死亡护盾、同时阵亡和金币奖励。
- 防御塔到遗迹的转换、两塔获胜、遗迹获胜、连续让过和先手权。
- 目标无效时的回滚、颜色限制、沉默限制、跨路伤害、保留先手。
- 商店扣款、商品售罄、物品牌组补货、商店契约、装备替换。
- 永久属性和装备跨复活保留、英雄等待轮次与初始技能冷却。
- 强制战斗推进、存档序列化、种子决策复现。

四组电脑对电脑的完整对局已结束，途中逐次检查单位位置唯一、属性有限、金币非负：

| 种子 | 天辉牌组 | 夜魇牌组 | 结束回合 | 操作步数 | 结果 |
|---|---|---|---|---|---|
| 77 | 苍翠锋线 | 暗月密谋 | 6 | 89 | 天辉胜利 |
| 195 | 赤红军团 | 黄金契约 | 5 | 72 | 天辉胜利 |
| 823 | 暗月密谋 | 苍翠锋线 | 6 | 97 | 夜魇胜利 |
| 419 | 黄金契约 | 赤红军团 | 6 | 86 | 平局 |

这些检查证明该实现能完成受测对局，不证明和原版服务器对全部组合逐条一致。当前具体差异列于 README。

## 数据更新边界

`research/open-set-*-set.json` 是卡牌 API 结构快照，`latest_*_english.txt` 与 `latest_*_schinese.txt` 是检索时的客户端文本记录。标准化时以正式客户端名称修正早期名称，以官方卡牌 ID 关联画作。

已显式处理的文本版本差异包括：斧王和嗜血狂魔基础属性、卓尔游侠属性与狂风目标、血怒费用、碧玉双匕、逃脱死亡的主动护盾、水晶室女每路首次施法回魔、露娜对随机月蚀充能、伐木机攻击者护甲以及莱恩的冷却缩短。

若继续提升还原度，应优先与原客户端逐条验证：光环失效时机、净化的效果来源、移动及战斗目标自由度、受伤后的死亡连锁和多英雄复制时的技能共享规则。


## 2026-09-08 棋盘与战斗交互改进

- 商店 `buy / hold` 清理旧事件；界面提交时只消费一次事件，存档不携带待播放事件。购物不再重播上一路的战斗、死亡及扣血效果。规则层拒绝购物和部署阶段直接调用 `combat()`。
- 战斗事件保存结算前的攻击者及目标。界面保留旧棋盘，使用独立浮动卡牌执行蓄力浮起、对撞或冲塔，命中时更新生命和伤害飘字，然后切换线路。动画期间锁定鼠标、快捷键及 AI 推进。
- 三路棋盘在连续横向空间中展示；总览与聚焦采用位置和缩放过渡，聚焦跟随下一线路，新回合部署完成后回到上路。聚焦仍显示攻击箭头，隐藏线路不进入键盘焦点。
- 单位按已有对齐列为锚点收拢，未配对的区间按双方顺序居中排列；单边部队收成紧凑队列。召唤选位保留尾部新增空位。该自动收拢规则属于本项目的重制实现，尚未逐条验证原客户端的所有落位边界。
- 保留敌方在上、己方在下、建筑在两端、箭头居中和底部手牌的桌面布局。短屏压缩装饰空白，1265 × 712 的浏览器实景中操作按钮和手牌可同时看到。

验证：原有 23 组引擎检查、新增 7 组战斗回归检查均通过；四组完整自动对局均结束（种子 77 / 195 / 823 / 419，分别 7 / 9 / 9 / 7 回合）。浏览器已验证上路到中路、中路到下路、下路到商店、购物到部署及新回合返回上路；总览与聚焦布局已截图检查。动画中观察到 3 张浮动攻击卡牌且让过按钮禁用。实际购买 10 金币物品：金币 100 → 90，手牌 5 → 6，所有单位及建筑生命不变，攻击浮层和伤害飘字数量均为 0。实际保留商品也验证无生命变化及旧特效重播。

可复现的浏览器场景：在项目根目录执行 `python -m http.server 8788 --bind 127.0.0.1`，打开 `http://127.0.0.1:8788/tests/battle-browser.html`，点击“继续上次对局”，再点“进行战斗”进入商店。此场景仅允许在独立 8788 端口初始化测试存档，避免覆盖正式游戏 8787 的存档。

当前实景验证覆盖桌面浏览器；未完成手机设备实测，也不宣称原版三维棋盘及战斗特效的逐帧还原。

## 参考图场景层

- `tools/build_scene.py` 对用户提供的参考画面进行透视校正，重铺木质战斗区并清空固定数值；导出聚焦和总览两种棋盘纹理、小兵画作及透明信使剪影。卡牌、生命、魔力、攻击预告均由实时 DOM 渲染。
- `dist/battle-scene.css` 提供森林／熔岩桌面、动态河流光泽、圆形属性、装备栏、致死叉号、牌堆与信使、扇形手牌、紫色英雄对阵开场、木箱商店及不同镜头布局。
- `dist/scene.js` 仅负责视觉表现，不修改游戏状态：命中环和火花、召唤落位、法术光轨、购物金币与飞牌。战斗时序仍由 app 的事件消费流程控制，购物不会触发旧战斗事件。
- 商店支持查看游戏板、返回商店及完成购物。开场可点击或使用空格、Enter、Escape 跳过，开场期间阻止对局操作和 AI 推进。

实景验证：1280 × 720 下检查聚焦与三路总览；实际观察攻击浮牌、禁用操作、结算后进商店、购买飞牌。商品说明正常显示；查看棋盘后返回商店，购买后完成购物可进入第 2 回合部署，浏览器无 error 日志。购买前后所有单位和建筑生命不变。23 组引擎、7 组战斗回归与四场完整自动对局通过。法术与召唤的新增视觉分支完成语法检查，尚未逐种卡牌实机覆盖。

## 原声与技能反馈

- `audio.js` 使用 HTMLAudioElement → Web Audio 音乐总线播放本地 MP3，切场景交叉淡化并释放旧音源；普通 render 不重启当前歌曲。四首战斗曲轮播，开场／胜利／失败单次播放。音乐和音效独立增益，施法短暂压低音乐，后台标签页暂停媒体时间。
- `feedback.js` 包装成功的 `play / activate`，在动作前后捕获目标、来源和结果，记录精确的技能名称和阵营。元数据写在原有事件及日志中，不改动战斗规则；目标死亡、转移或改变阵营后仍能复原受术对象。AI 和玩家共用同一条路径。
- 普通施法先保留旧棋盘、展示卡牌并高亮对象，随后播放命中和数字，最后更新布局并恢复 AI。跨路作用自动展开总览；伤害、治疗、增益、装备、控制使用不同的合成音效与视觉法阵。历史摘要不会被让过动作覆盖。
- `tests/feedback.test.cjs` 覆盖增益、敌方控制、致死目标、敌方主动技能、装备及非法操作回滚；`tests/audio.test.cjs` 覆盖曲目轮播、阶段映射、一次性片段、旧音源释放、静音／压低音乐、后台暂停及失败兜底。原有 30 组检查与四场完整对局在载入反馈模块后均通过。
- 实机用 `tests/skill-browser.html`（独立 8788 端口）验证，`?case=lethal / enemy / shop / victory / defeat` 分别提供致死施法、电脑主动技、商店和胜负场景。测试不写正式 8787 存档。已验证冰封禁制及其宙斯被动连带伤害、致死结果、防御架势、敌方莱恩死亡之指，以及可播放的战斗／商店原声。

浏览器进一步确认：真实战斗触发胜利后播放 Victory（30.14 秒，结束后停止）；失败后播放 Defeat（14.49 秒，结束后停止）；重新开局播放 Opening，跳过开场切回战斗音乐。Caravan 与 Two Towers 的媒体 readyState 为 4，播放时间持续增加。测试期间发现并修复了旧脚本缓存及测试夹具英雄引用问题；发布入口为变更资源添加版本号，最终页面无新增运行错误。

## 可观察、可调整的英雄部署与棋盘适配

- `board.js` 分离纯镜头几何与 `DeploymentPlan`。单路保留 1510 / 867 比例，三路及窄屏使用 670 / 510 比例；视口变化时重算可用空间，英雄面板的实际高度参与计算。没有把中心石雕、圆形底座作为九宫格的拉伸区，因此不会因切片拉伸再次变形。
- `board-layout.css` 统一使用 `--scene-*` 布局变量，单位尺寸随棋盘高度缩放。单路按钮中心使用原图底座坐标（1260, 792）或窄屏图坐标（550, 425）。按钮移出玩家信息面板的堆叠上下文，直接放在战场交互层；手牌容器空白处允许点击穿透，真正卡牌保持可交互。
- `board-input.js` 对空白区域区分单击／双击，避开技能目标、卡牌、阶段遮罩及结算锁。部署采用 Pointer Events 统一鼠标与触控拖动，超过拖动阈值才捕获指针，避免普通点击无法选中英雄。支持从待部署区拖入战线、从预览拖往另一条线、取消拖动和点击按钮替代操作。
- 预分路保存为 `s.deploymentPlan`，并非实际部署。确认前可修改／撤回／重置，不消耗随机数、不产生召唤事件、不提前恢复生命。确认统一调用部署与进入上路，失败则回滚整批操作。玩家可在部署期间缩放棋盘、查看单位详情；旧的居中遮挡式部署弹窗已替换为底部操作区。

验证：新增 6 组规划与几何检查通过，原有 23 组引擎及 7 组战斗回归检查通过。实机完成三位英雄拖动、预览改路、点击分路、保存后重新载入、统一确认；全景单击聚焦与双击往返正常。按钮实际命中为 `pass-button`，点击后推进 AI 并完成下一路切换，1280 × 720 下底座中心误差小于 1 px。浏览器检查覆盖 1280 × 720、390 × 844、844 × 390、2560 × 1080；短屏部署面板与棋盘保持间隔，超宽屏各路保持原图比例。触控拖动代码与鼠标共用路径，尚未进行实体触屏设备实测。

可复现实景：独立 8788 端口打开 `tests/deployment-browser.html`，点击继续上次对局。该夹具提供三位待回归英雄及不同塔血量；需要验证预分路持久化时，安排后进入同端口 `/dist/index.html` 并继续对局（直接刷新夹具会重置测试存档）。
