# 匹配码双人联机

## 使用

运行 `npm install`，再 `npm start`，或双击 `start-online.cmd`。要求 Node.js >=22.13（使用内置 SQLite）；Python 启动方式仍可用于离线单人，但不能承载联机。

1. 双方打开同一服务器的 `/online.html`。
2. 一人创建六位匹配码，另一人输入匹配码加入。
3. 每人选择自己浏览器保存的构筑或预设构筑，也可导入本项目格式的 JSON。
4. 双方确认准备后，进入原三路棋盘。各自的单位始终在自己的屏幕下方。
5. 出牌、技能、让过和战斗通过服务器同步；购物各自进行，双方确认后进入新回合。
6. 英雄分路可在确认前反复调整。服务器收齐两人方案后同时部署，不提前展示对方落点。

默认无离线倒计时或自动认输；暂时离开不会输掉对局。另一人可以执行自己的合法行动，轮到离线方后等待其返回。认输是明确操作。

## 恢复和本地构筑

匿名凭证保存在 `artifact.online.identity`；最近房间保存在 `artifact.online.room`。同一浏览器和访问地址再次进入大厅，可以找回自己参与的对局。新标签打开相同席位会接管连接，旧标签提示在新页面继续，不会互相反复重连。

匹配码只是入场码，不是已有席位的恢复凭证。清除站点数据、换浏览器或换域名/IP后，不能仅凭匹配码接管原席位。本版不包含跨设备身份恢复码或账号系统。

本地 `artifact.customDeck` 和原有单人 `artifact.match` 不会被联机写入覆盖；剧情存档 `artifact.campaign.v1` 保持独立。联机界面偏好使用 `artifact.online.ui.` 前缀。`localhost`、`127.0.0.1`、局域网 IP 是不同存储来源；想带走构筑，请先在原地址导出 JSON，然后在联机大厅导入。

## 部署

- `PORT` 默认 `8787`；`HOST` 默认 `0.0.0.0`。
- `ARTIFACT_DB` 默认 `server/data/online.sqlite`，指定时应使用稳定路径。
- 一个 Node 进程同时提供静态 `dist` 页面、HTTP 房间接口和 Colyseus WebSocket。
- `/health`、`/api/online/health` 返回服务就绪信息。
- 远程设备需要能连到服务器 TCP 端口。公网或反向代理场景需要转发 WebSocket；本次不配置路由器端口映射。
- SQLite 使用 WAL 和每操作事务持久化。停服后备份数据库最简单；运行中不要只复制主文件而遗漏 WAL。
- 本版为单进程房间服务，不支持多个 Node 实例共享数据库同时承载相同房间。
- 通信 SDK 由本机依赖通过 `/vendor/colyseus.js` 提供，不使用运行时 CDN。

## 管理员只读入口

服务器首次启动时生成数据库同目录下的 `admin-token`（32字节随机数，64位十六进制），访问地址为 `/_admin/<token>`。该地址不在玩家导航中，页面也不放在公开 `dist` 目录；错误地址返回404。请保存此地址，不要分享给其他玩家。管理员页面只读，查看在线访客、联机连接、当前房间及历史对局，支持分页与自动刷新，不显示玩家恢复凭证。

使用 `ARTIFACT_ADMIN_TOKEN_FILE` 可指定秘密文件的持久路径，也可用 `ARTIFACT_ADMIN_TOKEN` 指定32–128位URL安全随机值。替换秘密后重启服务可使旧入口失效。管理员地址本身相当于访问凭证。

所有正式游戏页面每25秒发送匿名活跃心跳，页面隐藏后停止；管理页以75秒内活跃访问与当前WebSocket连接去重统计。它区分单人页面、剧情、大厅和联机战斗；匿名标签是凭证的摘要，不是恢复凭证。静态或离线单人访问失败时，心跳不会影响游戏。

## 实现结构

`server/store.cjs` 保存匿名身份、房间席位、构筑、状态和最近操作编号。`server/match.cjs` 复用 `engine.js`、`effects.js` 与 `feedback.js`，不运行 AI，不计算画面动画。`server/index.cjs` 负责静态页面、HTTP 与 Colyseus 房间。

客户端发 `{id,revision,action,args}`；服务端按序执行，保存成功后发送包含版本号、席位、公开信息和本次事件的快照。每个席位保留最近 256 个操作编号防止近期重发重复执行；版本号额外阻止陈旧操作。错误会回传原因和最新快照。独立购物操作同时提交遇版本冲突时，后一个玩家会收到提示并可重试，不会误扣金币。

手牌与牌库内容按对手视角打码，分路方案只在双方确认后落地。这里提供基本游戏信息隐藏和席位校验，不承诺完整反作弊或高并发压测结果。

`online-client.js` 管理匿名身份与重连；`online.js` 提供大厅；`online-bridge.js` 将服务端席位投影到原棋盘的己方视角，复用 `app.js` 渲染、技能反馈与战斗动画。初次进入或重连直接恢复当前画面，不重播历史扣血。

## 验证

```bash
npm run test:online
node tests/engine.test.cjs
node tests/battle-regressions.cjs
node tests/deployment.test.cjs
node tests/campaign.test.cjs
node tests/campaign-deck.test.cjs
```

网络集成测试使用独立临时数据库与 8791 端口，覆盖两位真实 WebSocket 客户端、拒绝第三人、不同构筑、隐藏手牌、重复指令、过期版本、购物屏障、分路屏障、浏览器重连、服务重启与认输。不要在测试运行时占用 8791。

### 本次验证记录（2026-09-09）

- 联机测试：8 项房间逻辑、4 项浏览器通信单测、3 组棋盘适配测试及实际 HTTP/WebSocket 集成流程通过。
- 原单人规则 23 项、战斗回归 7 项、技能反馈 6 项、部署 6 项、剧情 19 项、剧情牌组 12 项通过；另完成 4 场确定性人机对局。
- 隔离的 8790 测试服务上，用 `127.0.0.1` 和局域网 IP 两个独立浏览器存储来源，实测创建/加入、不同构筑准备、第二席位冰封禁制指定敌方斧王、三路推进、购买旅者斗篷、双方分路、装备后双方生命显示一致、刷新、重启 Node 后自动重连和双方胜负结局。
- 1280×800 和 844×390 画面检查通过，横屏页面无横向溢出，让过按钮 44×44；原单人浏览器入口实测仍会运行电脑回合。
- 正式 8787 已切换为 Node 服务，原单人及剧情页面仍返回正常；8788 测试服务器继续仅监听本机。已存在的 TCP 8787 防火墙放行规则适用于 Node。

这些是本机双浏览器身份和本机经局域网地址的验证，不代表另一个物理设备或公网链路已经实测，也不是并发性能压测。

## 构筑分享码

构筑页点击“分享码”生成 AFD-XXXXXXXX；好友在同一服务器点击“导入分享码”，预览后确认。英雄顺序、主牌和物品完整保留。编辑器导入仅替换草稿，需手动保存；联机房间可直接导入选用。分享码是服务器持久化快照，后续编辑不改变旧码，与房间匹配码不同。原 JSON 导入导出仍可使用。

## 实时观战与赛后收藏

在联机大厅填写匹配码后点击“实时观战”。观众不占玩家席位，双方手牌、抽牌顺序与未确认分路不展示；可以等待开局、查看单位、切换线路并在断线后恢复。观众无法操作棋局。

对局结束点击“保存对手构筑”，在当前浏览器另存收藏，保留英雄顺序、主牌及装备，不覆盖当前自定义构筑。收藏可在单人对战、构筑页的“预设”列表和联机选牌中使用。

手机手牌：普通横滑滚动；长按后左右移动阅读图文说明；保持长按向上拖动，卡牌跟手移动，松到合法单位/空位/战线使用。松到无效位置取消。多目标牌完成第一个目标后，按提示继续选目标。

## 在线匹配

联机大厅点击“开始匹配”，每名玩家至少等待10秒，按等待顺序与其他正在排队的玩家配对。不足两人时持续等待，随时可以取消。找到对手后有30秒选择本机构筑并点击“确认构筑并准备”，双方均准备后自动开战。

准备超时：在线且已准备的玩家重新入队，未准备的玩家返回大厅；重新入队仍至少等待10秒。排队期间离线超过15秒移出队列，避免挂机占位。页面刷新可恢复排队、剩余准备时间及已锁定的构筑。对局开始后仍使用原来的自动保存、恢复及观战流程。

匹配通过服务器队列配对，无机器人补位，也不承诺缺少对手时的最长等待时间。手动创建房间、输入匹配码和单人人机玩法保留。
